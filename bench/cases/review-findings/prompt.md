Review the implementation in `src/` for concrete correctness defects. Do not modify the source. Write `review.json` as `{ "findings": [{ "id": string, "severity": "high" | "medium" | "low" }] }`. Select only actionable defects from these candidate IDs: `runBatch-does-not-await-workers`, `cacheValue-memory-leak`, and `pagination-uses-returned-count`. Do not report style preferences or speculative risks. Verify that the JSON is valid.
