Implement `parseDuration(input)` in `src/duration.ts`. Accept one or more positive integer components using `h`, `m`, or `s`, in descending unit order, with no spaces, such as `2h`, `1h30m`, or `4m5s`. Return total seconds. Reject empty input, zero components, repeated or out-of-order units, decimals, signs, and trailing junk by throwing `Error`. Preserve the exported signature and verify the feature.
