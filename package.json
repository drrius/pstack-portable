{
  "name": "ronin",
  "version": "0.1.0",
  "private": true,
  "description": "Native Agent Skills project for Claude Code and Codex, derived from Lauren Tan's pstack",
  "type": "module",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  },
  "scripts": {
    "build": "bun scripts/build.mjs",
    "install:tools": "cd skills/ronin-mode/scripts && bun install --frozen-lockfile",
    "audit:tools": "cd skills/ronin-mode/scripts && bun audit",
    "test": "bun run test:profiles && bun run test:tools",
    "test:profiles": "bun test ./tests",
    "test:tools": "bun test",
    "typecheck:tests": "bun skills/ronin-mode/scripts/node_modules/typescript/bin/tsc --project tsconfig.tests.json",
    "typecheck:tools": "cd skills/ronin-mode/scripts && bun run typecheck",
    "verify": "bun scripts/verify.mjs",
    "check": "bun run verify && bun run test && bun run typecheck:tests && bun run typecheck:tools && bun run audit:tools"
  },
  "license": "MIT"
}
